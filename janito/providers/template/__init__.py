"""Template provider configuration package.

This package is *not* a real provider: ``config.py`` is the documentation
template for writing a new provider's config entry.  See
:mod:`janito.providers.template.config` for the full reference of every
CONFIG option.
"""
