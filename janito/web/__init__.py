"""Janito Web UI package (alpha).

Provides a browser-based chat interface for Janito that exposes the full
agentic tool-calling experience (streaming responses, tool execution, MCP
services, skills).

This mode is in **alpha**: the interface, CLI flags, API endpoints and
WebSocket protocol may change in incompatible ways between releases.

Launched via ``janito --web``.
"""
